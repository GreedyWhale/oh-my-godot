
> Godot 的生命周期，是指一个对象从创建、进入 SceneTree、完成准备、持续运行，到离开 SceneTree 这一过程中，Godot 在特定阶段自动调用的一系列回调。

注：SceneTree 是由 Scene 组成的树；而 Scene 本身又是 Node 树，所以最终 SceneTree 同样是一棵 Node 树。

| 回调                        | 什么时候发生                | 理解                     |
| ------------------------- | --------------------- | ---------------------- |
| `_init()`                 | 对象刚被创建、属性初始化之后        | 最早期初始化                 |
| `_enter_tree()`           | Node 进入 `SceneTree` 时 | “我已经进入运行中的树了”          |
| `_ready()`                | 自己和所有子节点都完成进入与准备后     | “我的子节点已经准备好了，我可以开始工作了” |
| `_process(delta)`         | 每个渲染帧调用               | 普通逐帧逻辑                 |
| `_physics_process(delta)` | 按固定物理节奏调用             | 移动、物理相关逻辑              |
| `_exit_tree()`            | Node 即将离开 `SceneTree` | 清理、退出相关逻辑              |

顺序是：

```
创建对象
↓
_init()

进入 SceneTree
↓
_enter_tree()

准备完成
↓
_ready()

运行期间
↓
_process()
_physics_process()

离开 SceneTree
↓
_exit_tree()
```

需要注意的是：

- `_enter_tree()` 父节点先，子节点后
- `_ready()` 子节点先，父节点后

## 1. @onready

> `@onready` 是 GDScript 的一个注解，用来把某个成员变量的初始化延迟到当前 Node 即将执行 `_ready()` 的阶段。

```
_init()
↓
_enter_tree()
↓
@onready 变量初始化
↓
_ready()
```

这里需要注意的是 `@onready` 会先于 `_ready` 执行

但如果放到**父子节点关系**里，就要再加一层顺序。假设：

```
Game
└── Player
```

那么是：

```
Game._enter_tree()
↓
Player._enter_tree()
↓
Player 的 @onready 初始化
↓
Player._ready()
↓
Game 的 @onready 初始化
↓
Game._ready()
```

