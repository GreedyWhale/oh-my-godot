## 1. Node

> **Node 是 Godot 的基本构建单元：它是一个能够拥有自身属性、方法、Signal 和生命周期行为的对象，同时可以与其他 Node 建立父子关系，从而组成树状结构。**

目前学到的 Node 类型

| Node类型           | 理解                             | 适用场景                                     |
| ---------------- | ------------------------------ | ---------------------------------------- |
| Node2D           | 具有 2D 位置、旋转、缩放能力的基础节点          | 根节点                                      |
| CharacterBody2D  | 由代码控制移动、参与物理碰撞的角色节点            | 游戏人物，怪物，NPC                              |
| StaticBody2D     | 通常固定不动的物理实体                    | 墙，地面，障碍物                                 |
| Area2D           | 检测其他物体进入、离开、重叠的区域              | 道具拾取范围，技能范围                              |
| CollisionShape2D | 给物理节点提供碰撞形状                    | 决定“碰撞/检测范围具体长什么样”                        |
| Marker2D         | 在 2D 世界里标记一个位置                 | 怪物生成区域                                   |
| Timer            | 负责计时，并在时间到时发出 `timeout` Signal | 毒雾，比如角色进入就会持续掉血的场景                       |
| CanvasLayer      | 创建独立于游戏世界的画布层                  | HUD，在游戏过程中始终浮于游戏画面上方、向玩家展示核心状态与关键信息的视觉元素 |
| Label            | 显示文字                           | 提示文字                                     |
| ProgressBar      | 用图形方式显示一个数值范围中的当前值             | HP条，进度等                                  |
| Button           | 提供可点击的 UI 控件                   | 菜单按钮、操作按钮                                |
| VBoxContainer    | 自动把子节点纵向排列                     | HUD 信息纵向排版                               |
| MarginContainer  | 给子节点提供边距空间                     | UI 外边距、内容留白                              |

Node 的部分能力并不是自生就有的，还需要配合其他类型的 Node 来实现，比如: `StaticBody2D` 表示**一个静态物理实体**，它的碰撞区域是由 `CollisionShape2D` 提供的，你甚至可以实现一个看起来是矩形形状墙的物体，但是它的的碰撞区域却是圆形的组合。

## 2. Scene

> 节点是 Godot 的基本构建单元。它们可以被指定为另一个节点的子节点，从而形成树状结构，节点树被称为**场景**

场景有点类似我理解的编程中**类**的概念，比如我可以将“金币”相关的节点都集中在一起形成一个 `Coin` 场景，然后在其他节点中通过 `preload` 方法引入, 然后进行实例化。

场景可以多次实例化，每次实例化的对象都是独立的。

```
var COIN_SCENE: PackedScene = preload("res://coin/Coin.tscn")

var coin = COIN_SCENE.instantiate()
var coin1 = COIN_SCENE.instantiate()
var coin2 = COIN_SCENE.instantiate()
var coin3 = COIN_SCENE.instantiate()
```

`Coin.tscn` 就是一个场景文件，文件后缀是 `tscn`

场景还可以通过直接拖动的方式加载进另一个场景中

## 3. Script

> Script 是一种用于扩展对象功能的代码资源；它可以为对象增加属性、方法、Signal 和自定义行为。


比如：角色的攻击方法，血量这类原始节点中不存在的属性和方法，可以通过 Script 进行扩展。


还可以扩展 `Signal` , `Signal` 有点类似事件订阅通知系统，某个场景发出一个通知，其他场景可以订阅这个通知。

```
# player.gd

signal died # 定义 Signal

died.emit() # 触发 Signal

# game.gd

# 订阅通知，当 player 发出 died Signal 的时候会自动执行这个方法
func _on_player_died() -> void:
	print("GAME OVER")
```


## 4. Resource

> Resource = 一个可以被保存、加载、引用和共享的数据对象；
> 
> 它通常为 Node、Script 或其他 Resource 提供数据，而不是自己作为游戏对象存在于节点树中。

简单来说就是一些通用的配置或者共享的数据，比如一个角色的参数配置：

```
# player_stats.gd

class_name PlayerStats
extends Resource

@export var max_hp: int = 100
@export var speed: float = 200.0
```

这是一个 `Script` 类型的文件，用于定义数据结构，当创建好这个文件后，在 Godot 的 FileSystem 面板就可以右键新建一个 PlayerStats 类型的资源（Resource）了

创建好的 Resource 文件的后缀是 `.tres`

假设你现在有如下的节点树结构：

```
Player 
└── player.gd               # 场景对应的脚本
└── player.tscn             # 场景
└── player_stats.gd         # resource 的数据结构
└── player_stats.tres       # 通过 player_stats.gd 创建的 resource 文件
└── fast_player_stats.tres  # 通过 player_stats.gd 创建的 resource 文件，但是数值和player_stats 不一样
```

`player_stats.gd` 的内容是：

```
class_name PlayerStats
extends Resource

@export var max_hp: int = 100
@export var speed: float = 200.0
```

只有两个属性，fast_player_stats 和 player_stats 区别在于 speed 不同。

如果在 `player.gd` 这样写：

```
@export var stats: PlayerStats
```

那么在 `player.tscn` 场景的根节点中就会出现一个名叫`stats`的插槽，可以在软件的右侧面板中看到。

这时候可以通过将 `player_stats.tres` 文件直接拖动过去，然后其他场景如果有用到 Player，那么默认的 stats 就是 `player_stats` 配置的值。

比如我在 `game` 这个场景用到了 `player`，我创建两个 `player`，分别是 `player1` 和 `player2`，我就可以在 `game` 场景下选中 `player2`，将它的 stats 插槽的值改成 `fast_player_stats`。

```
player.tscn
默认：
stats → normal_stats.tres
        │
        ├── Game 中 Player A
        │   没修改
        │   → stats.tres
        │
        └── Game 中 Player B
            覆盖了
            → fast_stats.tres
```

除了这种方式之外也可以通过 `preload` 和 `load` 的方式建立引用